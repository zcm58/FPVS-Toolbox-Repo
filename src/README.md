# src package notes

This directory contains the application source. Keep durable developer setup
notes in `../docs/agent/guides/development.md`, not in this package-local README, so future
agents and maintainers have one place to check before changing code.

Use `../ARCHITECTURE.md` and `../docs/agent/agent-index.md` from the repository root to
choose focused source files and verification commands.
